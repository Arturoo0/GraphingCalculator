local environment = {
  abs = math.abs,
  acos = math.acos,
  asin = math.asin,
  atan = math.atan,
  ceil = math.ceil,
  cos = math.cos,
  deg = math.deg,
  exp = math.exp,
  floor = math.floor,
  fmod = math.fmod,
  log = math.log,
  pi = math.pi,
  rad = math.rad,
  sin = math.sin,
  sqrt = math.sqrt,
  tan = math.tan,
  max = math.max,
  min = math.min,
}

return environment
